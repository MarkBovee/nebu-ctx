## 1. Skill Surface And Flow

- [x] 1.1 Add a new user-invokable `project-bootstrap` skill with clear trigger phrases and a preview-first flow
- [x] 1.2 Wire the skill into the maintained skill install/discovery path so it is available to supported agents again

## 2. Bootstrap Engine

- [x] 2.1 Implement a deterministic project bootstrap preview that summarizes stack, entrypoints, tests, infra, modules, and workflow signals from existing `ctx_*`, metadata, and index inputs
- [x] 2.2 Implement explicit apply behavior that stores approved bootstrap facts through canonical memory paths with stable identities and provenance

## 3. Docs, Help, And Verification

- [x] 3.1 Update `README.md`, `docs/MEMORY.md`, and CLI help/cheatsheet text to explain project mapping/bootstrap workflow and its explicit preview/apply behavior
- [x] 3.2 Add focused tests for skill availability, preview/apply behavior, and docs/help discoverability
- [x] 3.3 Validate the change with targeted tests and any relevant bootstrap workflow checks before handoff
