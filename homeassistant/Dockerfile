# This Dockerfile is used for local testing only.
# Production HA deployments pull the pre-built GHCR image via the 'image:' field in config.yaml.
ARG NEBU_CTX_VERSION=latest
FROM ghcr.io/markbovee/nebu-ctx:${NEBU_CTX_VERSION}
